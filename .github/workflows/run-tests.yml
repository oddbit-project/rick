name: Tests

on:
  push:
    branches: [ master, development ]
  pull_request:
    branches: [ master, development ]

jobs:
  security-audit:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4
    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.12'
    - name: Install pip-audit
      run: |
        python -m pip install --upgrade pip
        pip install pip-audit
    - name: Run pip-audit
      run: |
        pip install -r requirements.txt
        # Ignore GHSA-4xh5-x5gv-qwph: pip tarfile extraction vulnerability
        # This affects pip itself (build tool), not runtime dependencies
        # Fix is planned for pip 25.3 - remove this ignore once available
        pip-audit --desc --ignore-vuln GHSA-4xh5-x5gv-qwph

  build:

    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ['3.10', '3.11', '3.12', '3.13', '3.14']

    steps:
    - uses: actions/checkout@v4
    - name: Set up Python ${{ matrix.python-version }}
      uses: actions/setup-python@v4
      with:
        python-version: ${{ matrix.python-version }}
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip wheel setuptools
        pip install -r requirements-dev.txt
        if [ -f requirements.txt ]; then pip install -r requirements.txt; fi
    - name: Run tox targets for ${{ matrix.python-version }}
      run: |
          ENV_PREFIX=$(tr -C -d "0-9" <<< "${{ matrix.python-version }}")
          TOXENV=$(tox --listenvs | grep "^py$ENV_PREFIX" | tr '\n' ',') python -m tox
